  /// Returns a new [{{{classname}}}] instance.
  {{{classname}}}({
  {{#vars}}
    {{!
        A field is required in Dart when it is
        required && !defaultValue in OAS
    }}
    {{#required}}
      {{^defaultValue}}required {{/defaultValue}}
    {{/required}}
    {{^required}}
      {{^isNullable}}
        {{^defaultValue}}required {{/defaultValue}}
      {{/isNullable}}
    {{/required}}
    this.{{{name}}}
    {{#required}}
      {{#defaultValue}} = {{#isEnum}}{{^isContainer}}const {{{enumName}}}._({{/isContainer}}{{/isEnum}}{{{defaultValue}}}{{#isEnum}}{{^isContainer}}){{/isContainer}}{{/isEnum}}{{/defaultValue}}
    {{/required}}
    {{^required}}
      {{^isNullable}}
        {{#defaultValue}} = {{#isEnum}}{{^isContainer}}const {{{enumName}}}._({{/isContainer}}{{/isEnum}}{{{defaultValue}}}{{#isEnum}}{{^isContainer}}){{/isContainer}}{{/isEnum}}{{/defaultValue}}
      {{/isNullable}}
    {{/required}},
  {{/vars}}
  });